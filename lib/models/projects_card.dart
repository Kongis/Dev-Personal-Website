import 'package:flutter/material.dart';
import 'package:personal_website/models/button_template.dart';
import 'package:personal_website/models/models.dart';
import 'package:sizer/sizer.dart';

//For Future

/*Widget buildCard(int index) => Padding(
  padding: EdgeInsets.only(left: 2.w, right: 2.w),
  child:   Container(
    height: 20.h,
    width: 20.w,
    decoration: BoxDecoration(color: Colors.red, borderRadius: BorderRadius.circular(50)),
    child: Column(
      children: [
        Padding(padding: EdgeInsets.only(), child: Text(projectdata[index].title),),
        Padding(padding: EdgeInsets.only(), child: Text(projectdata[index].subtitle),),
        //Padding(padding: EdgeInsets.only(), child: Image(image: AssetImage(projectdata[index].image))),
        Padding(padding: EdgeInsets.only(), child: ProjectButton(text: "Website"),)
  
      ],
    ),
  ),
);*/